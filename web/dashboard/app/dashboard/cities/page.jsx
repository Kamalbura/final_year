import CitiesGrid from '../../../components/CitiesGrid';

export const metadata = {
  title: 'All Cities - Air Quality Dashboard',
  description: 'View air quality index for all cities',
};

export default function CitiesPage() {
  return <CitiesGrid />;
}
